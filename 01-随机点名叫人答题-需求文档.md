# 需求文档 01 · 随机点名叫人答题

> 归属：电教委中立立项 ｜ 版本：v1.0 ｜ 日期：2026-08-29
> 前置阅读：00-总体架构与公共模块.md（部署形态、roster.json 名单格式、公共非功能需求）

## 1. 目标场景

课堂提问环节，教师想找学生回答问题。按下手机上的「随机点名」按钮，大屏播放抽取动画并定格在一名学生身上；教师随即可以派题（叫人答题）。全程学生看着大屏，公平、有仪式感、防"点名偏心"质疑。

## 2. 角色与入口

| 角色 | 设备 | 入口 |
|---|---|---|
| 教师 | 手机/电脑浏览器 | `http://<一体机IP>:8080/ctrl.html` |
| 学生 | 大屏（一体机/投影） | `screen.html` 全屏，被动观看 |

## 3. 功能需求

### F1 随机抽取

| 编号 | 需求 | 优先级 |
|---|---|---|
| F1-1 | 教师点击「随机点名」，大屏进入抽取动画（滚动的名字瀑布或转盘），2~4 秒后定格 1 名学生，姓名+照片占位放大展示 | P0 |
| F1-2 | 抽取按 roster.json 的 `weight` 加权随机；`pickedCount` 越高权重自动越低（公平衰减） | P0 |
| F1-3 | 「不复读机」开关：开启后本轮已点过的学生不再重复，全班点完自动重置 | P0 |
| F1-4 | 教师可按组抽取：只从「A组」等指定分组中抽 | P1 |
| F1-5 | 支持一次抽取多人（2/3/4 人，用于小组答题） | P1 |
| F1-6 | 动画可静音/换音效（上课场景可关闭音效） | P2 |

### F2 叫人答题（点名后的派题动作）

| 编号 | 需求 | 优先级 |
|---|---|---|
| F2-1 | 定格结果下方出现两个大按钮：「请回答」「跳过TA」；教师手机端有同样按钮 | P0 |
| F2-2 | 点击「请回答」：大屏进入答题计时（默认 60 秒，教师端可调 30s/1min/2min/不限），大屏显示大号倒计时，时间到闪烁提示 | P0 |
| F2-3 | 点击「跳过TA」：当前学生记 skippedCount+1，自动抽下一名（连抽逻辑同 F1） | P1 |
| F2-4 | 答题结束后教师可标记「答对 / 答错 / 未答」，计入学生统计 | P1 |
| F2-5 | 大屏展示本节课点名录（侧边栏小字滚动），下课自动清空 | P2 |

### F3 控制端辅助

| 编号 | 需求 | 优先级 |
|---|---|---|
| F3-1 | 控制端显示当前班级、可选分组、抽取参数（是否复读机、人数） | P0 |
| F3-2 | 结果历史：本节课每次抽取的姓名与时间戳列表 | P1 |
| F3-3 | 多班级切换（调用公共名单 currentClass） | P1 |

### F4 名单管理

| 编号 | 需求 | 优先级 |
|---|---|---|
| F4-1 | 支持从文本/Excel 粘贴导入名单（一行一名，可选"姓名,组,权重"三列） | P0 |
| F4-2 | 支持在控制端直接增删改学生、改权重 | P1 |
| F4-3 | pickedCount/答题统计自动写回 roster.json（或独立 stats.json），可在控制端查看班级"被点分布" | P2 |

## 4. 关键流程（主路径）

```
教师手机点「随机点名」
  → 控制端 emit: {cmd:'pick', class:0, group:null, count:1, noRepeat:true}
  → ws-server 校验并广播 screen:startRoll
  → 大屏播放动画（名字滚动/转盘），同时控制端按钮锁定防连点
  → 动画结束，大屏定格学生X，pickedCount[X]+1 落盘
  → 大屏浮现「请回答 / 跳过TA」，控制端同步
  → 教师点「请回答」→ 大屏倒计时开始
  → 教师点「答对/答错/未答」→ 记录，流程结束，按钮解锁
```

## 5. 界面要点

- 大屏定格页：学生姓名 ≥ 屏高 1/4，居中，配班级与组别小字；背景暗化突出姓名
- 控制端按钮 ≥ 64px 高，单手可达；核心操作（点名/请回答）永远在首屏
- 动画时长教师可在控制端调（快 2s / 标准 3s / 仪式感 5s）

## 6. 验收标准

1. 断网（无公网）环境下完整走通主路径，指令延迟 ≤ 200ms
2. 50 人班级连抽 20 次，无重复（开启不复读机时），pickedCount 分布合理（高权重者显著更易被点中）
3. Excel 粘贴 50 行名单 ≤ 10 秒完成导入并立即生效
4. 一体机浏览器全屏 F11 下无滚动条、无白边、最后一排可读姓名
5. 教师手机锁屏再亮屏，重连后状态与当前大屏一致（断线重连补偿）
