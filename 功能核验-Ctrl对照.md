# 点将台 · 微信小程序控制端 功能核验书（对齐 Ctrl 控制页）

> 核验日期：2026-09-05 · 对照基准：`app/public/ctrl.html` + `ctrl.js`（v=37）
> 结论：**小程序端已实现 Ctrl 控制页全部可迁移功能**（桌面专属项除外，见 §6），
> 全部静态校验通过；云端命令动作覆盖率 42/45（未覆盖 3 项为 Ctrl 亦未使用的内部动作，见 §4）。

---

## §1 功能对照总表（Ctrl → 小程序）

| Ctrl 区块 | 功能点 | 小程序实现位置 | 状态 |
|---|---|---|---|
| 顶栏 | 云端访问密码鉴权（401 重输、失败可见） | `pages/connect` + `utils/djt.js` | ✅ |
| 顶栏 | 班级下拉选择 / 🔒锁定标记 / 切班免二次输密码 | `components/classbar`（会话缓存 `djUnlock:<rid>`） | ✅ |
| 顶栏 | ➕新建班级 / ✏️改名 / 🗑删除当前班（锁班需密码、删完回首页） | `classbar` 第二行按钮 | ✅ |
| 顶栏 | 考试模式开关 | 点名页 `roll.wxml` 开关 | ✅ |
| 顶栏 | 连接状态徽标 | 各页顶「连接失败/连接中」兜底卡 + connect 页状态 | ✅ |
| 顶栏 | ⬇下载桌面版 | 小程序不适用（桌面安装包外链无法内嵌） | ➖ 见 §6 |

### 点名答题（roll）
| Ctrl | 小程序 | 状态 |
|---|---|---|
| 抽取范围：全班/分组 chips | 同上 `s.groups` | ✅ |
| 抽取人数 1–4 | 同上 | ✅ |
| 不复读机 + 重置本轮 | 同上 | ✅ |
| 🎲随机点名（权重/不重复/跳过请假） | `roll` cmd + 高频轮询等动画 | ✅ |
| 今日请假：徽标计数/名单/编辑勾选/清空 | roll 页 请假卡（含人数红标） | ✅ |
| 点名结果：姓名大卡 + 🎤请回答/⏭跳过 | 结果卡 + ActionSheet 选倒计时 | ✅ |
| 倒计时选择 30/60/120/不限 | ActionSheet（30/1分/2分/不限） | ✅ |
| 答题中倒计时（∞/低电量/时间到）+ 答对/答错/未答 | 卡内 leftTxt 秒级 tick + 三键 | ✅ |
| 本节课记录（✅/❌/未答/跳过 + 时间） | 记录卡带彩色 tag | ✅ |

### 传呼（page）
| Ctrl | 小程序 | 状态 |
|---|---|---|
| 姓名/学号搜索、多选（以学号区分同名） | 搜索框 + 候选列表 + 已选 chips | ✅ |
| 去处 chips + 新增去处 | 同上 | ✅ |
| 找哪位老师 / 留言 ≤30 | 同上 | ✅ |
| 📢发送传呼 | `page` cmd | ✅ |
| 当前传呼：✅已到 / ❌撤回 | 当前传呼卡 | ✅ |
| 今日传呼记录（最新在前、✅/撤回/…） | 记录卡倒序展示 | ✅ 本次修 |

### 名单（roster）
| Ctrl | 小程序 | 状态 |
|---|---|---|
| 分组管理（添加/×删除，学生不受影响） | 分组卡（点组可筛选） | ✅ |
| 粘贴导入新班（一行一名、多格式） | 导入卡 + 成功后问是否切班 | ✅ |
| 单个添加（姓名/学号/组别/权重） | 添加卡 | ✅ |
| 学生行：学号内联改 / 权重内联改（**0=不点他**）/ ×删除 | 行内 input 失焦保存 | ✅ 本次修权重 0 |
| 📌 一键加入/取消今日请假 | 行内 📌 红色开关 | ✅ 本次加 |
| 被点次数 / 组别展示、清零统计 | 行 meta + 清零按钮 | ✅ |

### 课表（tt）
| Ctrl | 小程序 | 状态 |
|---|---|---|
| 公告栏 发布/清除（≤120 字，按班保存） | 公告卡 | ✅ |
| 备忘录：添加/勾选完成/删除/清已完成/大屏作业栏开关（带时间） | 备忘卡 | ✅ 本次补时间 |
| 节次：上午/下午 ±(1..8)、早读/晚托开关、清空课表 | 节次卡 | ✅ |
| 每节上课/下课时间（失焦自动保存） | 时间卡 | ✅ |
| 今日答题统计（答出/未答出/共N人）+ 清空 | 统计卡 | ✅ |
| 一周课程表格（直接填、失焦保存） | 网格（周一..周五表头） | ✅ 本次修表头 |
| 节次标签：上午N节/下午N节/早读/晚托 | 同上（标签对齐 Ctrl） | ✅ 本次修 |

### 设置（set）
| Ctrl | 小程序 | 状态 |
|---|---|---|
| 班级密码：状态显示/设置/修改(验旧)/移除(验旧) | 设置卡 | ✅ |
| 抽取动画时长 快/标准/仪式感 | chips | ✅ |
| 大屏显示课表开关 | switch | ✅ |
| 上课自动考试模式总开关 | switch | ✅ |
| 大屏播报方式 提示音/AI/都要 | chips | ✅ |
| 音量滑条（拖动实时百分比、松手保存） | slider bindchanging+bindchange | ✅ 本次修 |
| 连接信息（班级房/云端域名/PIN 是否已存） | 连接信息卡 | ✅ |
| 外观 跟随系统/浅色/深色 | 小程序不适用 | ➖ 见 §6 |
| 本机开机自启动 | 桌面版专属，不适用 | ➖ 见 §6 |

### 锁屏与切班（全局）
| Ctrl | 小程序 | 状态 |
|---|---|---|
| 打开加密班 → 全屏锁屏（非云端密码提示） | `components/lockoverlay` 全屏盖住 | ✅ |
| 锁屏「切换其他班级」入口 | 锁屏下方 → connect 页 | ✅ |
| 解锁成功缓存（本会话免重输） | `djUnlock:<rid>` 缓存 | ✅ |
| 点名结果大弹窗（屏幕居中、新结果才弹） | 小程序以结果卡内联大字展示（等效） | ✅ 差异说明 §6 |

---

## §2 本次补齐内容清单

1. **名单页权重允许 0**（Ctrl 语义：0=今天不点他；原代码 `w>0` 会拒存 0 值）；非法输入 toast 提示。
2. **名单页行内 📌 今日请假快捷开关**（红色=请假中，点按即发 `setAbsent`，与 Ctrl stuList 一致）。
3. **传呼记录倒序**（最新在前，同 Ctrl `reverse()`）。
4. **传呼显示「姓名·学号」**（同 Ctrl：区分同名），当前传呼补充发出时间、留言/找老师合并显示。
5. **课表页节次标签对齐 Ctrl**：上午1节…/下午1节…/早读/晚托（原为笼统“第N节”）。
6. **课表周表头修正**：周1…周5 → 周一…周五。
7. **备忘录列表补时间显示**（Ctrl 同步可见 sentAt）。
8. **设置页低频轮询（6s）**：外部（大屏/其他端）改动音量/动画/开关可回流显示。
9. **音量滑条拖动实时显示百分比**（bindchanging），松手才保存。
10. **点名页请假红徽标计数**（对齐 Ctrl absentBadge 样式底色）。
11. **已选学生 chip 改蓝 on 态**（原误用红色 lock 样式）。

## §3 静态校验结果（全部通过，可复核命令见 §7）

| 校验项 | 结果 |
|---|---|
| 全部 .js 语法 `node --check`（21 个文件） | ✅ PASS |
| 全部 .json 可解析（app/页面/组件/项目配置） | ✅ PASS |
| WXML 禁止表达式扫描（方法调用/Math/箭头/join…） | ✅ 0 处 |
| WXML 标签平衡（含自闭合，精确逐标签） | ✅ 全部文件 OK |
| WXML 事件处理器 ↔ JS 方法存在性 | ✅ 无缺失 |
| 组件 `triggerEvent` ↔ 页面 bind 事件（switch/unlocked） | ✅ 对齐 |

## §4 云端动作覆盖率

- 小程序实际调用动作：**42 个** → 云端 worker.js 全部支持（`comm` 差集为空）。
- 云端共 45 个动作，小程序未调用的 3 个及原因：
  - `classSwitch`：Ctrl 用 URL 跳转前的解锁+落位；小程序改用「目标房 `unlockClass` 解锁 + `setRoom` + 重载」等效路径，无需该动作。
  - `replaceRoster` / `setRollStyle`：Ctrl 界面亦未暴露的后端/高级动作，不在对照范围。

## §5 真机/模拟器验收步骤（人工走查路径）

前置（一次性）：
1. 开发者工具 → 云开发 → 开通并创建环境，复制环境 ID 填入根目录 `config.js` 的 `CLOUD_ENV`。
2. 左侧 `cloudfunctions/proxy` → 右键 **上传并部署：云端安装依赖**。
3. 工具「清缓存 → 重新编译」。

| # | 走查步骤 | 预期结果 |
|---|---|---|
| 1 | 首次打开 connect 页，输入云端访问密码保存 | 出现「已连接 · 当前班级」，班级列表可点 |
| 2 | 顶栏 ➕新建班级 → 起名并设密码 | 列表尾部出现新班 |
| 3 | 点带锁班级 chip | 弹输入班级访问密码；输对进入，输错有提示 |
| 4 | 切到已解锁过的锁班 | 免输密码直接进入（会话缓存） |
| 5 | 点名页选范围/人数/不复读 → 🎲随机点名 | 动画后出姓名大字；「请回答」选 30 秒后卡内倒计时走动，答对/答错/未答可点 |
| 6 | 传呼页搜索学生多选、选去处、填老师 → 📢发送 | 当前传呼卡出现；✅已到 / ❌撤回可用；记录最新在前 |
| 7 | 名单页：点学生行 📌 | 名字旁出现红色“📌请假”，点名页请假计数同步 +1 |
| 8 | 名单页把某学生权重改为 0 | 保存成功无报错（点名时该生不参与） |
| 9 | 名单页粘贴导入新班 → 导入 | 提示成功并可「去使用」切到新班 |
| 10 | 课表页：发布公告、加备忘、勾选完成、节次 +1、填格子课程 | 各自即时生效；表头显示 周一…周五；行标签为 上午1节… |
| 11 | 设置页调音量滑条 | 拖动时百分比实时变，松手后大屏音量变化 |
| 12 | 打开任意 tab 断网/停云函数 | 顶部红字原因卡 + 「重试」+「⚙连接设置」，不再白屏 |
| 13 | 双端并行：小程序点名/传呼 | 大屏端（screen.html 或桌面版）同步收到点名/传呼弹窗 |

## §6 已知边界与差异（有意为之）

1. **桌面专属 3 项不迁移**：开机自启动、外观主题切换、⬇下载桌面版 —— 均为 Windows 桌面版/浏览器环境能力，小程序无对应运行时，已在设置页明示。
2. **点名结果大弹窗**：Ctrl 以网页居中 modal 呈现；小程序改为结果卡内联大号展示 + 滚动定格，交互链（请回答→计时→判定）完全一致。
3. **AI 播报**：语音在**大屏端**播放（sound/ai/both 为“大屏提示方式”），小程序不发声，属预期。
4. **班级密码输入框**：小程序 `wx.showModal` 无 password 样式，锁屏页已自绘 password input（lockoverlay），管理弹窗中密码为普通输入（与 Ctrl `prompt()` 明文一致）。
5. 轮询 vs SSE：小程序无 EventSource，用 3–6s 轮询取状态，功能等价、延迟略增。

## §7 校验命令（可复跑）

```bash
# JS/JSON
for f in $(find . -name "*.js" -not -path "*/node_modules/*"); do node --check "$f" || echo FAIL $f; done
# WXML 非法表达式（应无输出）
grep -rnE "\{\{[^}]*(\w+\s*\(|=>|Math\.|\.join\(|\.indexOf\(|\.map\(|\.filter\(|new Date|console\.)" --include=*.wxml .
# 动作覆盖率
grep -rhoE "cmd(Of)?\('[a-zA-Z]+'" --include=*.js . | grep -oE "'[a-zA-Z]+'" | tr -d "'" | sort -u
```

## §8 文件清单（本次改动）

| 文件 | 改动 |
|---|---|
| `pages/roster/roster.js/.wxml/.wxss` | 权重 0 允许+NaN 提示、📌请假快捷、abs 预处理 |
| `pages/page/page.js/.wxml` | 记录倒序、姓名·学号、发出时间、chip 蓝态 |
| `pages/tt/tt.js/.wxml` | 上午/下午节标签、周一..周五表头、备忘时间 |
| `pages/set/set.js/.wxml` | 6s 轮询、音量拖动实时百分比 |
| `pages/roll/roll.js/.wxml` | 请假人数红徽标 |
