# 课堂互动大屏系统 · 总体架构与公共模块

> 立项文档 00 ｜ 归属：电教委中立立项 ｜ 版本：v1.0 ｜ 日期：2026-08-29
> 配套文档：01-随机点名叫人答题-需求文档.md、02-喊人到办公室-需求文档.md

## 1. 一句话定位

教师用手机/电脑浏览器当遥控器，教室大屏（一体机/投影浏览器全屏）当展示屏，**无公网、无云端**，在教室局域网内实时完成「随机点名叫人答题」「喊人到办公室」两类指令。

## 2. 部署形态（唯一部署物 = 教室一体机本机进程）

```
┌─────────────────────────────── 教室一体机（大屏） ───────────────────────────────┐
│  本机进程：npx http-server ./app -p 8080   （或 python -m http.server 8080）      │
│  本机进程：node ws-server.js               （Socket.IO，约100行，仅监听局域网）   │
│  大屏浏览器全屏：http://localhost:8080/screen.html                                │
└──────────────────────────────────────────────────────────────────────────────────┘
            ▲ 局域网 WiFi（教师手机连同一 WiFi）
┌───────────┴──────────────┐
│ 教师手机/电脑浏览器        │
│ http://<一体机IP>:8080/ctrl.html │
└──────────────────────────┘
```

- 控制端 → 大屏通道：Socket.IO（WebSocket），指令延迟 < 200ms
- 大屏无人值守：展示页断线自动重连；一体机重启后双击桌面「启动.bat」即可恢复
- 严格无进程方案（降级路线）：仅 screen.html 单页 + 触屏操作，功能降级见各文档

## 3. 公共技术选型（均为开源组件，可离线）

| 组件 | 用途 | 许可证 | 引入方式 |
|---|---|---|---|
| Socket.IO（v4） | 控制端↔大屏双向实时通道 | MIT | 本地文件引入，不依赖 CDN |
| http-server（Node） | 静态页面服务 | MIT | npx 本地运行 |
| lucky-canvas（v2） | 大屏转盘/老虎机动画 | Apache-2.0 | 本地打包进 app 目录 |
| 原生 HTML/CSS/JS | 页面本体 | — | 零框架，双击可调试 |

> 所有依赖一次性下载后拷贝进 `app/` 目录，之后**永久离线可用**。

> **实现说明（M1 交付时更新）**：实际实现采用 **Node 内置 http + SSE(Server-Sent Events) 零依赖方案**替代 Socket.IO——功能等价（服务端下行用 SSE，控制端上行用 POST），且无需 `npm install`，整个 app 文件夹 U 盘拷贝即部署，离线可靠性和部署速度更优。性能指标不变。

## 4. 公共数据模型：名单文件 roster.json

两个功能共用一份名单，与页面同目录，记事本即可编辑：

```json
{
  "classes": [
    {
      "name": "初一(3)班",
      "groups": ["A组", "B组"],
      "students": [
        { "name": "张三", "group": "A组", "weight": 1.0, "pickedCount": 0 },
        { "name": "李四", "group": "B组", "weight": 2.0, "pickedCount": 0 }
      ]
    }
  ],
  "currentClass": 0
}
```

- `weight`：被抽中权重（默认 1.0；值越大越容易被点到）
- `pickedCount`：累计被点次数，自动写入，用于"公平加权"（点过的自动降权）
- 多班级：切换班级即切换名单子树

## 5. 公共页面职责

| 页面 | 文件 | 职责 |
|---|---|---|
| 控制端 ctrl.html | 手机/电脑打开 | 选班级、发指令（点名/答题/办公室）、查看结果历史 |
| 大屏 screen.html | 一体机全屏 | 接收指令、播放动画、展示结果、显示二维码（手机扫码入） |
| ws-server.js | 一体机后台 | 转发指令、广播状态，落盘 pickedCount |

## 6. 公共非功能需求

| 编号 | 需求 | 指标 |
|---|---|---|
| N1 | 离线运行 | 拔网线/无公网全功能可用 |
| N2 | 指令延迟 | 按下按钮到大屏响应 ≤ 200ms（局域网） |
| N3 | 大屏兼容 | 一体机自带浏览器（Chromium 内核）及 Edge/Chrome，分辨率自适应 1080P/4K |
| N4 | 并发 | ≥ 3 个控制端同时在线（含备用电脑） |
| N5 | 数据安全 | 名单与统计仅存本机，不上传任何数据 |
| N6 | 部署耗时 | 新教室从拷贝到可用 ≤ 5 分钟 |
| N7 | 字号 | 大屏结果展示：最后一排可读（≥ 120px 字号档） |

## 7. 里程碑

| 阶段 | 内容 | 验收物 |
|---|---|---|
| M1 | 公共框架：ws-server + 双页面骨架 + 名单加载 | 局域网内手机按按钮，大屏有响应 |
| M2 | 功能①随机点名叫人答题（见 01 文档） | 演示视频 + 课堂试用 |
| M3 | 功能②喊人到办公室（见 02 文档） | 演示视频 + 课堂试用 |
| M4 | 打包：启动.bat、二维码海报、使用说明 1 页 | U 盘拷贝式分发包 |
