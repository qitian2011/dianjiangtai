# 电教委 · 课堂互动大屏系统

> 教室一体机 + 教师手机，零依赖的课堂互动点名系统。Node.js 实现，无需 npm install，U 盘拷贝即可运行。

## 这是什么

一套部署在教室一体机（Windows）上的课堂互动工具：

- **大屏展示端**（screen.html）：一体机浏览器全屏显示，负责点名动画、倒计时、传呼卡片
- **教师控制端**（ctrl.html）：教师手机连接教室同一 WiFi 后打开，负责点名、传呼、名单管理

两端通过 SSE 长连接 + POST 指令实时通信，局域网延迟 <200ms。

## 功能一览

| 模块 | 功能 |
|---|---|
| 🎯 随机点名 | 加权随机（权重0=永不抽中）、不复读机、按组抽取、一次1-4人、倒计时、答对/答错/未答标记、跳过连抽 |
| 🎰 老虎机动画 | rAF 驱动真滚轮、二次减速、逐列停止弹性吸附，定格=真实结果 |
| 🔊 AI 语音播报 | Web Speech API + Windows 离线中文语音，点名/传呼/答题播报，传呼含留言 |
| 📢 传呼 | 多选学生、自定义去处/留言、时长自定义（0-3600秒）、撤回/已到闭环、考试模式静默 |
| 📋 名单管理 | Excel 导入、增删学生、自定义分组、班级增删改、权重行内编辑、一键请假（跨天自动失效） |
| 🏫 多班级 | 班级可添加/删除/重命名，当前班级切换 |
| 📱 二维码 | 待机页自动生成多张控制端地址二维码，扫码即连 |
| 🌓 三态主题 | iOS/macOS 苹果风格深色，浅色/深色/跟随系统可切换 |
| 🔒 考试模式 | 大屏只显示角落小字、无声音，防打扰 |

## 快速开始

1. 一体机安装 [Node.js](https://nodejs.org/)（任意版本，无需任何 npm 包）
2. 双击 **`app/启动.bat`** → 自动启动服务并打开大屏页（浏览器按 F11 全屏）
3. 手机连教室同一 WiFi，浏览器打开大屏待机页底部显示的地址（`http://一体机IP:8080/ctrl.html`）
4. 开始使用：点名页点「随机点名」→ 大屏动画定格 → 「请回答」计时 → 标记答对/答错/未答

> 局域网连不上？双击 `app/放行防火墙.bat`（需管理员）放行 8080 端口。
> 远程访问（在家/办公室管理名单）？见 `app/Tailscale远程连接指南.md`。

## 文件结构

```
├── 00-总体架构与公共模块.md      # 需求文档
├── 01-随机点名叫人答题-需求文档.md
├── 02-喊人到办公室-需求文档.md
└── app/
    ├── 启动.bat                  # 一键启动
    ├── 放行防火墙.bat            # 放行 8080 端口
    ├── server.js                 # 服务端（静态页 + SSE + 指令处理 + 名单落盘）
    ├── roster.json               # 名单数据（真实数据，已 gitignore，首次运行自动生成示例）
    ├── roster.example.json       # 示例名单模板
    ├── README-使用说明.md        # 详细使用说明
    ├── Tailscale远程连接指南.md  # 远程访问教程
    └── public/
        ├── screen.html/js        # 大屏展示端
        ├── ctrl.html/js          # 教师控制端
        ├── theme.js              # 三态主题
        ├── style.css             # 公共样式
        └── vendor/               # 本地化第三方库（qrcode 等）
```

## 技术要点

- **零依赖**：`server.js` 仅用 Node 内置模块（http/os/fs），不需要 `npm install`
- **实时通道**：SSE 下行 + POST 上行，浏览器断线自动重连
- **数据持久化**：全部在 `roster.json`，自动保存；删除即恢复示例数据
- **多网卡适配**：服务自动列出所有网卡的候选地址，插 USB 共享/开热点后地址自动新增

## 隐私说明

真实学生名单（`app/roster.json`）包含个人数据，**已通过 `.gitignore` 排除，不会上传到本公开仓库**。如需备份名单数据，请复制 `roster.json` 到私有仓库或本地存档。

## License

MIT © qitian2011
